[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Handle single page application routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
